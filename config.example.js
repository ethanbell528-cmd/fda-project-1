// Copy this file to config.js (which is gitignored) and paste your free
// The Odds API key (https://the-odds-api.com). Without a key the site falls
// back to the odds carried in ESPN's public scoreboard feed.
window.SITE_CONFIG = {
  ODDS_API_KEY: "",
};
